{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "cross-env AWS_PROFILE=serverlessStack sst test",
    "start": "cross-env AWS_PROFILE=serverlessStack sst start",
    "build": "cross-env AWS_PROFILE=serverlessStack sst build",
    "deploy": "cross-env AWS_PROFILE=serverlessStack sst deploy",
    "undeploy": "cross-env AWS_PROFILE=serverlessStack sst remove",
    "test-d": "sst test",
    "start-d": "sst start",
    "build-d": "sst build",
    "deploy-d": "sst deploy",
    "undeploy-d": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.91.0",
    "@types/aws-lambda": "^8.10.70",
    "aws-sdk": "^2.857.0",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "@aws-cdk/core": "1.91.0",
    "@serverless-stack/cli": "0.9.12",
    "@serverless-stack/resources": "0.9.12",
    "lambda-multipart-parser": "^1.0.1",
    "uuid": "7.0.3"
  }
}
